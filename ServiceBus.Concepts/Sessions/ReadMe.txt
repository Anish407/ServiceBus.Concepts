Use sessions when message need to arrive in order or if the messages need to be grouped and processed on FIFO basis. create a session with a sesion id 
and put the same session id for the messages.


